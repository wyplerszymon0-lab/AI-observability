{
  "name": "ai-observability",
  "version": "1.0.0",
  "description": "Observability layer for LLM API calls — tracks cost, latency, tokens and generates reports",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev":   "ts-node src/observer.ts",
    "test":  "vitest run"
  },
  "dependencies": {
    "openai":  "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node":     "^10.9.0",
    "typescript":  "^5.0.0",
    "vitest":      "^1.0.0"
  }
}
